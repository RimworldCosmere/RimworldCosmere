<?xml version="1.0" encoding="utf-8" ?>
<Patch>

  <!-- ThinkNode_ConditionalMentalState.Satisfied compares the def by reference, so a custom mental
       state matches none of vanilla's five branches. Without this the koloss enters bloodlust and
       then stands there with no job giver, which reads as the state doing nothing at all.

       Copied from the Berserk branch at SubTrees_Misc.xml:170-182. An Add whose XPath matches
       nothing fails silently, so if a koloss ever goes into bloodlust and just idles, check that
       this matched before looking anywhere else. -->

  <Operation Class="PatchOperationAdd">
    <xpath>Defs/ThinkTreeDef[defName="MentalStateCritical"]/thinkRoot/subNodes</xpath>
    <order>Prepend</order>
    <value>
      <li Class="ThinkNode_ConditionalMentalState">
        <state>Cosmere_Scadrial_MentalState_KolossBloodlust</state>
        <subNodes>
          <li Class="ThinkNode_Priority">
            <subNodes>
              <li Class="JobGiver_Berserk" />
              <li Class="JobGiver_WanderAnywhere">
                <maxDanger>Deadly</maxDanger>
              </li>
            </subNodes>
          </li>
        </subNodes>
      </li>
    </value>
  </Operation>

</Patch>
