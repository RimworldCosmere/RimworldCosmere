using UnityEngine;

namespace Cosmere.System.Scadrial.UI.Feruchemy;

// The dial and the strip sit next to each other, so their fills have to move together.
internal static class FeruchemyPalette {
    internal static readonly Color TapFill = new Color(0.659f, 0.435f, 0.290f);
    internal static readonly Color StoreFill = new Color(0.373f, 0.549f, 0.627f);
    internal static readonly Color CompoundTint = new Color(0.851f, 0.667f, 0.286f);
}
